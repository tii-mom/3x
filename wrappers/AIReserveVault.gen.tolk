// Auto-generated wrapper for contract 'AIReserveVault'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/common/messages"
import "@contracts/reserve/AIReserveVault"

struct AiReserveVault {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun AiReserveVault.fromStorage(toShard: AddressShardingOptions? = null): AiReserveVault {
    val stateInit = ContractState {
        code: build("AIReserveVault"),
        data: createEmptyCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return AiReserveVault { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun AiReserveVault.fromAddress(address: address): AiReserveVault {
    return AiReserveVault { address };
}

/// Deploys the contract to the blockchain
fun AiReserveVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun AiReserveVault.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun AiReserveVault.getVaultData(self): VaultDataReply {
    return net.runGetMethod(self.address, "get_vault_data");
}

fun AiReserveVault.getBucketReleased(self, bucketId: int): (coins, coins) {
    return net.runGetMethod(self.address, "get_bucket_released", [bucketId]);
}

fun AiReserveVault.sendSetEmissionController(
    self,
    from: address,
    queryId: uint64,
    controller: address,
    config: SendParams = {},
): SendResultList {
    return self.sendAny(from, (SetEmissionController { queryId, controller }).toCell(), config);
}

fun AiReserveVault.sendSetVaultJettonWallet(
    self,
    from: address,
    queryId: uint64,
    jettonWallet: address,
    config: SendParams = {},
): SendResultList {
    return self.sendAny(from, (SetVaultJettonWallet { queryId, jettonWallet }).toCell(), config);
}

fun AiReserveVault.sendPauseReserveVault(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    return self.sendAny(from, (PauseReserveVault { queryId }).toCell(), config);
}

fun AiReserveVault.sendUnpauseReserveVault(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    return self.sendAny(from, (UnpauseReserveVault { queryId }).toCell(), config);
}

fun AiReserveVault.sendReleaseReserveToAgent(
    self,
    from: address,
    queryId: uint64,
    agentId: uint256,
    recipient: address,
    amount: coins,
    stage: uint8,
    bucketId: uint8,
    config: SendParams = {},
): SendResultList {
    return self.sendAny(
        from,
        (ReleaseReserveToAgent { queryId, agentId, recipient, amount, stage, bucketId }).toCell(),
        config,
    );
}
